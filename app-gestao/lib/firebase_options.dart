// Placeholder gerado manualmente.
//
// Antes de rodar o app, gere o arquivo real com o FlutterFire CLI:
//   dart pub global activate flutterfire_cli
//   flutterfire configure --project=<SEU_PROJETO_FIREBASE>
//
// O comando acima sobrescreve este arquivo com as chaves reais
// (as chaves de app Firebase não são secretas; a segurança vem das Rules).

import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'firebase_options.dart ainda não foi gerado. '
      'Rode: flutterfire configure --project=<SEU_PROJETO_FIREBASE>',
    );
  }
}
